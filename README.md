# aipi

AI package

## Usage with Next.js

```js
const nextConfig = {
    experimental: {
        // packages resolved at runtime
        serverComponentsExternalPackages: ["pdf-parse"],
    },
};
```
